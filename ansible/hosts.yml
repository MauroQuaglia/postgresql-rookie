all:
  hosts:
    pg-vagrant:
      ansible_host: localhost
      ansible_user: vagrant
      ansible_port: 2222
      ansible_ssh_private_key_file: .vagrant/machines/pg-vagrant/virtualbox/private_key
